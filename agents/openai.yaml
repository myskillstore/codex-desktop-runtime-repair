interface:
  display_name: "Codex Windows Runtime Repair"
  short_description: "Safely diagnose missing newer Codex models on Windows."
  default_prompt: "Use $codex-windows-runtime-repair to safely diagnose why Codex Desktop on Windows is missing GPT-5.6 or newer models after an update."
